services:
  app:
    build: .
    container_name: site-photo
    ports:
      - "8080:3000"
    volumes:
      - ./server/data:/app/server/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
